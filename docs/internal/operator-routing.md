# Operator Routing

This internal note keeps launch-routing details out of the public README.

Before running paid, external, or data-bearing work, check the matching run card, config, budget, artifact paths, and approval state. Launch commands and approval bounds live in `run_cards/`.
